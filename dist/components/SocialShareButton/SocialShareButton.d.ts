/// <reference types="react" />
import { SocialShareButtonTypes } from "./types";
declare const SocialShareButton: ({ social, link, name, message, scale, target, width, disabled, ...props }: SocialShareButtonTypes) => JSX.Element;
export default SocialShareButton;
