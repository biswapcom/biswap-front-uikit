/// <reference types="react" />
import { SocialShareButtonTypes } from "./types";
declare const SocialShareButton: ({ social, link, name, message, scale, target, width, withGift, disabled, ...props }: SocialShareButtonTypes) => JSX.Element;
export default SocialShareButton;
