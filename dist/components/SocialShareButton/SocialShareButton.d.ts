import React from "react";
import { SocialShareButtonTypes } from "./types";
declare const SocialShareButton: ({ social, link, name, message, scale, target, width, withGift, disabled, ...props }: SocialShareButtonTypes) => React.JSX.Element;
export default SocialShareButton;
