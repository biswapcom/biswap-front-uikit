/// <reference types="react" />
export declare const PrevButton: ({ enabled, onClick, }: {
    enabled: boolean;
    onClick: () => void;
}) => JSX.Element;
export declare const NextButton: ({ enabled, onClick, }: {
    enabled: boolean;
    onClick: () => void;
}) => JSX.Element;
